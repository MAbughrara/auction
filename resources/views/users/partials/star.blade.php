<button type="button"
        class="btn btn-default btn-grey btn-{{$size}} @if($arg > $val) btn-warning @endif"
        aria-label="Left Align">
    <span class="glyphicon glyphicon-star" aria-hidden="true"></span>
</button>